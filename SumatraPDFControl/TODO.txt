TODO (First version):

Nuget Package
  - SumatraPDFControl version 1.0.0-alpha published on Nuget.org problems:
    - x86\SumatraPDF and x64\SumatraPDF isn't copy to output host application folder. https://docs.microsoft.com/en-us/nuget/reference/nuspec#specifying-files-to-include-in-the-package
    - include readme.md on package (https://docs.microsoft.com/en-us/nuget/reference/msbuild-targets#packagereadmefile)
Choose correct version of SumatraPDF.exe (32bits / 64bits) based on:
    string arch = System.Environment.GetEnvironmentVariable("PROCESSOR_ARCHITECTURE", EnvironmentVariableTarget.Machine);
Print - Direct
Throw exception on error loading document
Test ContextMenu Control events
Review control inherited properties / events to hide those are not used and implement others* 
Analyze and implement possible to send other messages from SumatraPDF Canvas WndProc to SumatraPDFControl 		 
  - Page: first, previous, next, last 
  - Select All
  - Others?
Special keys events 
  - Block WM_SYSCHAR message (handled by FrameOnSysChar on SumatraPDF.cpp) because ALT+Space can give user control of current plugin window 
  - Treat ALT key
LastPage get property
Global Config - auto update, etc
Documentation
  - Commenting all public methods, properties and events, etc
  - Generate documentation using https://github.com/EWSoftware/SHFB or https://dotnet.github.io/docfx/
  - Publish documentation on github pages like http://ewsoftware.github.io/SHFB or https://marcoscmonteiro.github.io/SumatraPDFControl/
Do a revision in GEDVISA PDFXChange used properties
   
TODO (Future versions):

Print Control:
 - Events: onPrint, onPrintEnd
 - Methods: AbortPrint, MayCloseWindow
Toc:
  - ContextMenu SumatraPDFControl for toc - Create a property for tocContextMenu
Others:
  - Create function to get Canvas, Toc and toolbar positions in Frame position to use by mouse click events 		 
  - Page rotation event is more complicated to implement because current Window information (WindowInfo*) does not exists in DisplayModel sumatrapdf object. 
    So its impossible to send PluginHostCallBack message without replicate this call in all points of source code calling method DisplayModel::RotateBy.
  - Implement FileOpened and StartupFinished events